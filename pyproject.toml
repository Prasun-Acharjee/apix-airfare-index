[project]
name = "apix"
version = "0.1.0"
description = "Real-time Airfare Price Index for the CPI Transport sub-group"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
